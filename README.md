# ProfitMatrix AI

This is a local static clone of the referenced site, updated with the brand name `ProfitMatrix AI` and the `How it works` section removed.

## Notes

- The hero image is configured as `hero.jpg` in the project root.
- Replace `hero.jpg` with your uploaded image file to show your actual image in the page.

## Run

Open `index.html` in a browser to view the site.
